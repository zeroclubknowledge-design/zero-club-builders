# Services

Shared API clients and integration code live here.

Use this package for Supabase calls, auth/session helpers, storage helpers, and other external platform integrations. Feature-specific orchestration can live under `src/features/*/services` when it is owned by one product area.
