# Components

Reusable presentational components live here.

Business logic, data fetching, and feature-specific state should move into the matching `src/features/*` package. Design-system primitives stay in `src/components/ui`.
