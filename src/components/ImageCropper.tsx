// Replaced by MediaCropper, which handles pictures and video and gives the
// media the whole screen instead of a 300px box inside a dialog.
export { MediaCropper as ImageCropper } from "./MediaCropper";
