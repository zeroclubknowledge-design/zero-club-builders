# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local
.env
.env.*

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Generated Files
.vercel/
.npm-cache/

# ---------------------------------------------------------------------------
# Android / Bubblewrap
#
# android/.gitignore covers these too; repeated here so the rules still apply
# if key material is ever created outside android/. Losing android.keystore
# means losing the ability to update the Play listing, and leaking it means
# someone else can sign builds Android accepts as Zero Club.
# ---------------------------------------------------------------------------
*.keystore
*.jks
*.p12
*.pepk
keystore.properties
android/.build/
android/.twa/
android/out/

# Build logs can capture tool output that includes signing passwords.
build-log.txt
*-build-log.txt
